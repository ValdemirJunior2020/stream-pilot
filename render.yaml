services:
  - type: web
    name: streampilot-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: JWT_SECRET
        sync: false
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: SHEETS_AUTH_URL
        sync: false
      - key: SHEETS_AUTH_SECRET
        sync: false
      - key: FRONTEND_ORIGIN
        sync: false